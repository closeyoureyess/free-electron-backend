package com.electron.handle.repository.adapter;

import com.electron.handle.entity.MessageEntity;

import java.util.UUID;

public interface MessageRepository extends Repository<UUID, MessageEntity> {
}
